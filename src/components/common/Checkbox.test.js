import { Screen, render } from "@testing-library/react";

import Checkbox from "./Checkbox";

describe("<Checkbox />", () => {
  it("Checkbox render test", () => {
    render(<Checkbox />);
  });
});
